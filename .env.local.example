# Google Cloud Vision API
# Get these from Google Cloud Console: https://console.cloud.google.com

# Option 1: For local development (path to credentials file)
GOOGLE_APPLICATION_CREDENTIALS=./google-credentials.json
GOOGLE_CLOUD_PROJECT_ID=your-project-id

# Option 2: For Vercel deployment (base64 encoded credentials)
# Run: cat google-credentials.json | base64
GOOGLE_CREDENTIALS_BASE64=your-base64-encoded-credentials

# Supabase (for future database integration)
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# NextAuth (for future authentication)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key
